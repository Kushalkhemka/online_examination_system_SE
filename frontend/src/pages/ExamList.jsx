import React from 'react';
import { Container, Typography } from '@mui/material';

function ExamList() {
  return (
    <Container>
      <Typography variant="h4">Exam List</Typography>
      <Typography>Display list of available exams</Typography>
    </Container>
  );
}

export default ExamList;
